var status = -1;

function start(mode, type, selection) {
    qm.sendNext("Thank you so much.");
    qm.completeQuest();
    qm.completeQuest(3186);
    qm.completeQuest(3187);
    qm.dispose();
}
function end(mode, type, selection) {
    qm.sendNext("Thank you so much.");
    qm.completeQuest();
    qm.completeQuest(3186);
    qm.completeQuest(3187);
    qm.dispose();
}