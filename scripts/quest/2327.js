var status = -1;

function start(mode, type, selection) {
    qm.gainExp(8000);
    qm.completeQuest();
    qm.dispose();
}

function end(mode, type, selection) {
    qm.gainExp(8000);
    qm.completeQuest();
    qm.dispose();
}